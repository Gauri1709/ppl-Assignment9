% subjects in department

dept_subj(comp_dept,dsa).
dept_subj(comp_dept,ppl).
dept_subj(comp_dept,dsgt).
dept_subj(comp_dept,dld).
dept_subj(math_dept,ode).
dept_subj(math_dept,uvc).


% departments having students

dept_student(comp_dept,tnb).
dept_student(math_dept,tnb).
dept_student(comp_dept,ssd).
dept_student(math_dept,ssd).
dept_student(comp_dept,pas).
dept_student(math_dept,pas).
dept_student(comp_dept,sgk).
dept_student(math_dept,sgk).


% teacher teaches these subjects

teacher_subj(sh,ppl).
teacher_subj(rs,dsgt).
teacher_subj(jw,dld).
teacher_subj(am,dsa).
teacher_subj(jtm,ode).
teacher_subj(dng,uvc).


% teachers in department
dept_teacher(D,T):-dept_suj(D,C),teacher_course(T,C).


% subjects taken by students

student_subj(S,C):-dept_student(D,S),dept_course(D,C).

% teacher teaches these students

teacher_student(T,S):-dept_student(D,S),dept_course(D,C),faculty_course(T,C).